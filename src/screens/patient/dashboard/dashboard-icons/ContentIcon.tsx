import { ContentPasteSvg } from '@components/ui/icons';
import { classes } from './index.tailwind';

export const ContentIcon = () => (
  <div className={classes.contentIcon}>
    <ContentPasteSvg />
  </div>
);
