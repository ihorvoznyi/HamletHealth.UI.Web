import { PeopleSvg } from '@components/ui/svg';
import { classes } from './index.tailwind';

const PatientIcon = () => (
  <div className={classes.container}>
    <PeopleSvg />
  </div>
);

export default PatientIcon;
