import { classes } from './index.tailwind';

const Head = () => {
  return (
    <div className={classes.container}>
      <h2 className={classes.title}>Define goal for: Omidon 10mg</h2>
      <p className={classes.paragraph}>Plz set frequently and type of scale of date</p>
    </div>
  );
};

export default Head;
